# Backend environment example
# Copy to .env and fill values for local development.
# For Vercel, set these in Project Settings → Environment Variables instead of using a local .env.

# MongoDB connection string (Atlas example)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/prajashakthi?retryWrites=true&w=majority

# A strong, secret key for signing JWTs (e.g., run `openssl rand -base64 32`)
JWT_SECRET=change-me-in-production

# Optional: set the port for local dev
PORT=5000

# Node environment (IMPORTANT: must be "production" on Vercel for secure cross-domain cookies)
NODE_ENV=production

# The exact frontend origin (no trailing slash) allowed by CORS in production/local
# Example: https://prajashakthi-vdp-form.vercel.app
CORS_ORIGIN=https://your-frontend-domain.vercel.app
